---
# tasks file for docker
- name: create image docker
  docker_image:
    name: CDApp
    build:
            path: /var/lib/jenkins/workspace/CDApp
            pull: no
            source: build
            state: present
            force_source: true
         - name: run

